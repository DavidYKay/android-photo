package cs213.selmon.androidphoto.util;

public class DatabaseHelper {
            
  //private void createTables() {
  //  db.execSQL("CREATE TABLE " + ALBUMS_TABLE_NAME + " ("
  //             + Albums._ID + " INTEGER PRIMARY KEY,"

  //             + Albums.NAME  + " TEXT,"

  //             + Albums.CREATED_DATE + " INTEGER,"
  //             + Albums.MODIFIED_DATE + " INTEGER,"

  //             + "FOREIGN KEY("+ Albums.CHANNEL_ID + ") REFERENCES " + PHOTOS_TABLE_NAME + "(" + Photos._ID + ")"
  //             + ");");

  //  db.execSQL("CREATE TABLE " + PHOTOS_TABLE_NAME + " ("
  //             + Photos._ID + " INTEGER PRIMARY KEY,"

  //             + Photos.NAME         + " INTEGER,"
  //             + Photos.DESCRIPTION  + " INTEGER,"

  //             + Photos.ACCESS       + " TEXT,"

  //             + Photos.MODIFIED_DATE + " INTEGER"
  //             + ");");
  //}
  //  
  //public DatabaseHelper() {
  //  mContext = getContext();
  //  mOpenHelper = new DatabaseHelper(mContext);
  //  //mSettingsHelper = new SettingsHelper(mContext);
  //  return true;
  //}

}
